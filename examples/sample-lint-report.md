# Sample Lint Report

## Healthy
Core navigation exists and the compiled root is clearly separated from raw notes.

## Findings
- one possible duplicate concept page
- two weakly linked pages in `concepts/`
- one backlog source in `Inbox/` that likely deserves source-only ingest first

## Priority Fixes
- P1: merge or canonicalize the duplicate concept naming
- P2: add index entry and cross-links for the weakly linked pages

## Backlog Candidates
- `Inbox/example-raw-note.md` -> likely source-only ingest

## Suggested next step
Run one manual ingest, then rerun audit-only lint.
